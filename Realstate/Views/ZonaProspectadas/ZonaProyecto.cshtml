@model IEnumerable<Realstate.Models.BaseDatos.ZonaProspectada>
@{

    Layout = "~/Views/Shared/_RealStateParcial.cshtml";
}
<p style="text-align:right">
    <a asp-action="Create" class="btn btn-danger">Nueva Área</a>
</p>
<div style="height: 400px;
top:inherit;
	border: 1px solid #ddd;
	background: #f1f1f1;
	overflow-y: scroll;">
    @foreach (var item in Model)
    {

        <div class="crow col-md-6 col-lg-12  main ">
            <!-- left column -->
            <div class="col-md-12">
                <!-- general form elements -->
                <div class="box box-primary">
                    <div class="box-body">
                        <div class="col-md-4">
                            <div class="form-group">
                                @Html.DisplayFor(modelItem => item.Name)
                            </div>
                            <div class="form-group">
                                <img src="@item.imagen" style="width:114px;height:58px" />
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="form-group">
                                @Html.DisplayFor(modelItem => item.CreationDate)
                            </div>
                            <div class="form-group">
                                @Html.DisplayFor(modelItem => item.Zona)

                            </div>
                            <div class="form-group">
                                @Html.DisplayFor(modelItem => item.StreetMain)
                                @Html.DisplayFor(modelItem => item.StreetSecundary)
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="form-group">
                                @Html.DisplayFor(modelItem => item.Usercreation)
                            </div>
                            <div class="form-group">
                                <input type="button" value="IR" class="btn btn-danger" />
                            </div>
                        </div>
                        @*<table border="0">
                                <tr>
                                    <td></td>
                                    <td colspan="2"</td>
                                    <td></td>
                                </tr>
                                <tr>
                                    <td>

                                    </td>
                                    <td>
                                        <table>
                                            <tr><td>@Html.DisplayFor(modelItem => item.Zona)</td></tr>
                                            <tr><td> @Html.DisplayFor(modelItem => item.StreetMain)</td></tr>
                                        </table>
                                    </td>
                                    <td>
                                        <table>
                                            <tr><td> 5</td></tr>
                                            <tr><td> @Html.DisplayFor(modelItem => item.StreetSecundary)</td></tr>
                                        </table>
                                    </td>
                                    <td>

                                    </td>
                                </tr>
                            </table>*@
                    </div>
                </div>
            </div>
        </div>




    }

</div>
@section Scripts {
    <script>

    </script>
}